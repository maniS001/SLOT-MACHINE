export enum AppDimension {
  width = 1366,
  height = 768,
}
export enum ReelProperties {
  row = 3,
  column = 5,
  dummyRows = row + 2,
  symbolHeight = 120,
  horizontalGap = 50,
  verticalGap = 75,
  symbolWidth = symbolHeight,
  totalSymbols = row * column,
}
